#! spec.yml
name: zero2prod
region: lon1
services:
  - name: zero2prod
    dockerfile_path: Dockerfile
    source_dir: .
    githup:
      branch: main
      deploy_on_push: true
      repo: GeographyJames/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xss
    routes:
      - path /